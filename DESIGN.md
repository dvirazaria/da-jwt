# סוגרים קופה — שפה עיצובית

מסמך עבודה לכל מי שמשפר את האפליקציה. יש לקרוא אותו יחד עם `HANDOFF.md` לפני שינוי בממשק.
הבסיס הוא העיצוב הקיים של קלוד ב־`kupa-sgura.html`, גרסה 43. הערכים להלן נלקחו מה־CSS ומהרכיבים בקוד; זהו תיעוד של הקיים, ולא אישור שבוצעה בדיקת נראות בכל מכשיר.

## אופי והיררכיה

אפליקציה עברית, RTL, שמיועדת קודם כול לטלפון ולשימוש במהלך ערב פוקר. המראה שקט, כהה ומדויק: רקע כמעט שחור, טקסט בהיר, טורקיז יחיד להדגשות. מצב בהיר הוא חלק מאותה שפה.

- ההיררכיה נוצרת באמצעות טיפוגרפיה, רווחים וקווי הפרדה דקים.
- שחקנים, העברות והיסטוריה מוצגים בשורות שטוחות. אין להוסיף כרטיס סביב כל שורה או קופסאות בתוך קופסאות.
- שם השחקן והסכום הם העיקר; תוויות ומידע משני נשארים שקטים.
- קפסולות משמשות לבחירה ולפעולות; עיגולים משמשים לכפתור הוספה ולאווטאר.
- יש לשמר את האופי הקיים גם בהוספת התחברות וסנכרון. אין להחליף אותו בעיצוב ברירת מחדל של ספריית רכיבים.
- אין להוסיף צבעי הדגשה, גרדיאנטים, איורי קזינו או קישוטי קלפים ללא החלטת עיצוב מפורשת.

## צבעים — להשתמש במשתני CSS

| משתנה | כהה — ברירת מחדל | בהיר | תפקיד |
| --- | --- | --- | --- |
| `--bg` | `#05070A` | `#F5F8F9` | רקע האפליקציה |
| `--text` | `#EAF4F4` | `#0C1519` | טקסט ראשי |
| `--dim` | `#64707A` | `#5A6870` | טקסט משני ואייקונים |
| `--faint` | `#39434C` | `#9AA8AE` | מידע חלש, placeholders, ערכי אפס |
| `--line` | `rgba(234,244,244,.09)` | `rgba(12,21,25,.12)` | קווי הפרדה וגבולות |
| `--accent` | `#35E0DC` | `#089E98` | בחירה, פעולות, רווח וסנכרון פעיל |
| `--accent-press` | `#8FF0EC` | `#0CC2BB` | הדגשת פעולה באינטראקציה |
| `--bad` | `#FF6D6D` | `#D4403E` | הפסד, אזהרה ופעולה הרסנית |
| `--bar-bg` | `rgba(13,17,22,.78)` | `rgba(245,248,249,.9)` | רקע סרגל הניווט |

מצב בהיר מוגדר באמצעות `:root[data-theme="light"]`. רכיב חדש צריך לעבוד בשני המצבים באמצעות אותם משתנים. אין להשתמש באדום כקישוט. אין להסתמך על צבע בלבד להסברת מצב או שגיאה.

## טיפוגרפיה

משפחת הגופנים: `"Heebo", "Segoe UI", Arial, sans-serif`. גוף הטקסט: `16px`, גובה שורה `1.5`, כיוון `rtl`. מספרים כספיים וסטטיסטיקות משתמשים ב־`font-variant-numeric: tabular-nums` למניעת קפיצות רוחב.

| שימוש | גודל | משקל / מאפיין |
| --- | --- | --- |
| כותרת ראשית | `clamp(26px, 7vw, 34px)` | `800`, גובה שורה `1.05`, ריווח אותיות `-0.02em` |
| כותרת כניסה | `34px` | `900`, גובה שורה `1.1` |
| מאזן גדול בפרופיל | `52px` | `900`, גובה שורה `1.1` |
| שם בפרופיל | `24px` | `800` |
| סכום העברה | `20px` | `900`, טורקיז |
| שם שחקן / שמות בהעברה | `17px` | `700` |
| יתרה בשורת חישוב | `16px` | `700`; אפס `300` |
| כפתור ראשי / בחירת סכום | `15px` | `700` / `500` |
| טקסט הסבר וסטטיסטיקה | `14px` | לרוב `300–400` |
| כותרת מקטע / מטא בשורה | `13px` | `500` / `300` |
| תוויות משניות | `11–12px` | `300–400` |

הקוד מבקש משקלים `800` בכמה מקומות, בעוד קישור Google Fonts טוען `300,400,500,700,900`. זהו פרט קיים שיש לבדוק אם משנים את טעינת הגופן, ולא לשנות אגב עבודה אחרת.

## פריסה וריווח

- עמוד יחיד ממורכז, רוחב מרבי `520px`; גם במחשב נשמרת הפריסה הצרה.
- `.wrap`: שוליים פנימיים אופקיים `18px`, למעלה `14px` בתוספת safe area, למטה `110px` בתוספת safe area כדי לפנות מקום לניווט.
- הכותרת שומרת `96px` בצד הסיום של הציר הלוגי עבור פקדי הפינה.
- שורת שחקן: גובה מינימלי `56px`, ריפוד אנכי `10px`, רווח `12px` בין פריטים וקו תחתון `1px`.
- שורת העברה: ריפוד אנכי `14px`; שורת היסטוריה: `12px`.
- מקטע תוצאות מתחיל במרווח `24px`; מקטע בפרופיל ב־`22px`.
- רווחים חוזרים ברכיבים: `4, 6, 8, 10, 12, 14, 16, 18, 22, 24, 26px`. אלה ערכים קיימים, לא סולם טוקנים פורמלי.
- להשתמש בתכונות לוגיות כמו `margin-inline-start` ו־`inset-inline` כשמתאים. לבדוק בפועל שמות עבריים, סימני מינוס, סכומים וחצים.
- שמות ארוכים רשאים להישבר (`overflow-wrap: anywhere`); סכומים ומטא קצרים נשארים בשורה אחת.

## רכיבים

### ניווט תחתון

קפסולה צפה עם שלוש לשוניות ראשיות, מימין לשמאל: **חברים, משחקים, פרופיל**. רוחב מרבי `470px`, מרחק מהצדדים `17px` ומהתחתית `10px`, רדיוס `999px`, ריפוד `6px 10px`, גבול דק וטשטוש רקע `18px`. מסכי **שולחן** ו־**חישוב** הם מסכים פנימיים של משחק פעיל ואינם מופיעים כיעדי ניווט ראשיים.

צל כהה: `0 8px 28px rgba(0,0,0,.35)`; בהיר: `0 8px 24px rgba(12,21,25,.14)`. זהו שימוש ממוקד בצל לרכיב הצף, ולא דפוס לכל התוכן.

לשונית פעילה מתרחבת (`flex-grow: 1.7`), מקבלת מילוי טורקיז וטקסט בצבע הרקע, וחושפת את התווית. לשוניות לא פעילות מציגות אייקון בצבע משני. גובה מינימלי `44px`; אייקון `22px`.

### קלטים וכפתורים

- קלט רגיל ללא קופסה ומילוי, עם קו תחתון בלבד; גובה מינימלי `42px`, ריפוד `8px 2px`. בפוקוס הקו נעשה טורקיז.
- כפתור ראשי וכפתור סגירת שולחן: קפסולה חלולה עם מסגרת וטקסט טורקיז, גובה מינימלי `48px`, ריפוד `12px 26px`. במכשיר שתומך ב־hover מקבלים מילוי טורקיז וטקסט בצבע הרקע.
- פעולת הוספת שחקן היא טקסט טורקיז פשוט לצד הקלט.
- כפתור הפלוס בשורת שחקן: עיגול `38px`, קו דק; בפתיחה מסתובב `45deg` ומקבל מילוי טורקיז.
- כפתור סכום: קפסולה עם גבול דק, גובה מינימלי `40px`, ריפוד `8px 18px`; בחירה פעילה מקבלת מילוי טורקיז ומשקל `700`.
- בחירת ״אחר״ מתרחבת לקפסולת קלט על רוחב שורת הסכומים.
- לשמר מצבי רגיל, hover, לחיצה, פוקוס, בחירה ואישור לפי סוג הרכיב.

### חישוב, פרופיל והגדרות

בחישוב, קלט הסכום ברוחב `104px`; היתרה מקבלת רוחב מינימלי `74px`. רווח טורקיז, הפסד אדום, אפס חלש. בהעברות הסכום מודגש ומופרד מהשמות באמצעות מרווח גמיש.

הפרופיל ממורכז, עם מאזן גדול ורשת מדדים בשתי עמודות, ברוחב מרבי `340px`. ההיסטוריה נשארת רשימת שורות עם מפרידים.

הכניסה וההגדרות הן שכבת מסך מלאה בצבע הרקע. התוכן ברוחב מרבי `380px`, עם ריפוד חיצוני `24px`. ההגדרות ממורכזות; אווטאר טורקיז עגול בגודל `84px`, קלטים עד `260px`. מתג ערכת הנושא בגודל `54×31px`, עם ידית `23px`.

### משחקים ומשחק פעיל

מסך **משחקים** הוא לוח הבית של עולם המשחקים. אין לו כותרת גדולה — הלשונית בניווט כבר אומרת ״משחקים״. התוכן המרכזי שלו ממורכז במסך: שורת ההסבר (`.games-home-lead`), מקטעי המשחקים, כרטיס המשחק והפעולות מיושרים למרכז, בעוד שכיוון הטקסט העברי נשאר RTL. נקודת הסנכרון נשארת בפינת ההתחלה של השורה העליונה, בדיוק באותו מקום כמו בפרופיל ובעמוד הקבוצה (אין למרכז את `.eyebrow` בשום מסך). בפינה הנגדית יושבים מתג ערכת הנושא וגלגל ההגדרות (`#settingsBtn`, אותו overlay כמו בפרופיל); עמוד הקבוצה לא מציג אותו כי יש לו ״הגדרות קבוצה״ משלו, והשולחן/הסגירה שומרים שם את האיפוס. עמודת התוכן (`.games-home-dash`) מתחילה `52px` מתחת לשורה העליונה, כך ששורת ההסבר והפעולות המהירות עוברות מתחת לערימת הפינה (~`86px`) ולא לצידה. הוא מציג משחק פעיל עם הפעולה המתאימה — ״כנס לשולחן״ או ״המשך סגירה״ — אזור קבוצות עם placeholder שקט ל־״צור קבוצה״, ואת פעולת היצירה היחידה הזמינה כרגע: ״משחק ללא קבוצה״. אין להוסיף כפתור כללי ״התחל משחק״ כשהלוח ריק. שני הכפתורים האלה (`.games-quick-actions`) יושבים ישירות מתחת לטקסט המוביל, בלי כותרת מקטע מעליהם — היא הייתה חוזרת על מה שהכפתורים כבר אומרים. קבוצה שעזבתי ובחרתי להסיר מהמכשיר (ראו ״תוספת — קבוצות״ למטה, `hiddenAt`) לא מופיעה כאן, גם לא בארכיון המקופל — רק אם היא נפתחת בקישור ישיר.

המשחק שומר `state.phase` באחד משלושה מצבים: `active`, `settlement`, `closed`. `appView` קובע את המסך הנוכחי: `friends`, `games`, `game`, `settle`, `profile`, או `group`. ב־`active` מוצג השולחן הקיים, ובתחתיתו ״סיים משחק״. זו פעולת לחיצה ארוכה רציפה של שנייה אחת עם מילוי אופקי עדין שמתקדם מקצה ההתחלה הימני לשמאל; שחרור מוקדם מחזיר אותו במהירות ומבטל את הפעולה. המילוי נשאר מלא לרגע קצר לפני המעבר. לאחר ההשלמה עוברים ל־`settlement` בלבד, בלי ליצור היסטוריה או חובות. בטעינה מחדש משחק פתוח חוזר לשלב המתאים; כשאין משחק פתוח נפתח הפרופיל.

ב־`settlement` נשמרים מסך החישוב, cashout, בדיקת האיזון, ההעברות וסטטוסי התשלום הקיימים. ״חזור לעריכת המשחק״ מחזיר ל־`active` ושומר את כל הנתונים לעריכה. רק ״סגור שולחן״ מעביר ל־`closed`, יוצר היסטוריה וחובות לפי הלוגיקה הקיימת, ומחזיר ללוח משחקים. רענון דף בוחר את המסך לפי `state.phase`; משחק סגור או מצב legacy ריק נפתחים בלוח משחקים ולא בשולחן ריק.

**שולחן ריק הוא לא משחק.** ״משחק ללא קבוצה״ פותח שולחן מיד, אבל כל עוד לא ישב בו אף שחקן הוא רק משבצת ריקה: הלוח לא מציג אותו כמשחק פעיל, הוא לא חוסם ״התחל משחק״ בקבוצה, ״סיים משחק״ לא מופיע בו, ויציאה ממנו (ללשונית ״משחקים״, ״פרופיל״ או חזרה לקבוצה) מוחקת אותו בשקט — בלי רשומת היסטוריה, כי המשחק מעולם לא התקיים. הקריטריון היחיד הוא `isGameOpen(state)`: לא דוגמה, `phase` פתוח, ולפחות שחקן אחד. שולחן עם שחקן אחד כבר מחזיק כסף (כניסה ראשונה) ולכן נחשב משחק לכל דבר. כשיש משחק כזה פתוח, הקפסולה ״משחק ללא קבוצה״ מוצגת `disabled` (`.games-quick-action:disabled` — אטימות `.45`, טקסט `--faint`, כמו `.btn-primary:disabled`) ומתחת לשתי הקפסולות שורת הסיבה השקטה הקיימת (`.games-primary-reason`) ״יש משחק פעיל אחר — סגור אותו קודם״ — אותו ניסוח שמופיע בעמוד הקבוצה. הדרך פנימה היא כרטיס המשחק הפעיל (״כנס לשולחן״/״המשך סגירה״); אין מצב שבו הקפסולה דורסת משחק שיש בו שחקנים.

### אייקונים

SVG קווי, `fill: none`, צבע `currentColor`, קצוות וחיבורים מעוגלים. עובי קו בדרך כלל `1.7–1.8`, ובחזרה `2`. גדלים קיימים `20–26px` בהתאם לתפקיד. לשמור על אותה משפחת אייקונים ושפה, ללא ערבוב באימוג׳י או אייקונים מלאים.

## תנועה ומשוב

התנועה קצרה ומסבירה שינוי מצב. אין להוסיף תנועות דקורטיביות גדולות.

- לחיצה: כיווץ ל־`.92` במשך כ־`.12s`; בלשונית `.94`.
- כניסת שורה: שקיפות והתרוממות מ־`translateY(6px)`, לרוב `.3–.5s`.
- פתיחת תפריט סכומים: מעבר רשת `0fr → 1fr` במשך `.28s`.
- כניסת קפסולות סכום: `.32s`, עם השהיות מדורגות `.04–.24s`.
- התרחבות קלט ״אחר״: `.3s`, מ־`scaleX(.28)`.
- עדכון סכום: הדגשה בטורקיז וכיווץ/הגדלה עד `1.18` במשך `.55s`.
- משוב שגיאה קיים: רעד אופקי `5px` במשך `.35s`.
- נקודת סנכרון פעילה: טורקיז וזוהר עדין במחזור `2.6s`.
- מעבר ערכת נושא: `.3s`.
- משוב hover שמשנה רקע או מילוי מופעל רק תחת `@media (hover: hover)`, כדי שלא יישאר דבוק אחרי מגע.
- תחת `prefers-reduced-motion: reduce` כל האנימציות והמעברים מבוטלים.

## התנהגות שהיא חלק מהעיצוב

- קניית צ׳יפים מאושרת בשתי לחיצות. לשמר את בחירת הסכום, שלב האישור והסגירה המונפשת; מנגנון `menuClosing` מונע חיוב כפול.
- פעולות הרסניות משתמשות במצב אזהרה ואישור נוסף בתוך הממשק. אין להמיר אותן ל־`alert` או `window.confirm`; אלה גם מוגבלים בארטיפקט של Claude.
- כאשר מקלדת נפתחת, סרגל הניווט מוסתר באמצעות `.kb-open` כדי למנוע הפרעה ב־iOS PWA.
- סמל החלפת ערכת הנושא מציג את המצב שאליו עוברים.
- נתוני דוגמה מסומנים במפורש; אין לטשטש את ההבדל בינם לבין מידע אמיתי.
- הניסוח בעברית קצר, ישיר ויומיומי: ״מי אתה?״, ״המשך״, ״מי משלם למי״. לשמור על המונחים הקיימים ועל תוויות שמבהירות את תוצאת הפעולה.

## נגישות ובדיקת שינוי עתידי

לשמר `aria-label` לפקדי אייקון ולקלטים, ניווט מקלדת ופוקוס ברור. לכפתורים קיים outline טורקיז `2px` עם מרחק `3px`. מידות הרכיבים המתועדות הן המצב הקיים; הן אינן אישור עמידה בתקן נגישות.

האפליקציה עובדת במצב אנכי בלבד. בהתקנה באנדרואיד הומניפסט ומנגנון הדפדפן מנסים לנעול ערכה זו; באייפון הנעילה לא מתבצעת, לכן בתצוגת רוחב קצרה במסך מגע מוצגת שכבה שקטה וחוסמת עם ההודעה ״האפליקציה עובדת במצב אנכי״ והנחיה לסובב חזרה.

לפני השלמת שינוי ממשק יש לבדוק:

- מצב כהה ובהיר, מסך טלפון צר ותצוגה רחבה.
- שלוש הלשוניות, כניסה והגדרות ככל שהשינוי נוגע בהן.
- שמות ארוכים, סכומים גדולים, יתרות חיוביות/שליליות/אפס ומצב ריק.
- תפריט סכומים, ״אחר״, אישור כפול ומניעת לחיצות חוזרות.
- מקלדת פתוחה, safe areas ושום תוכן שמוסתר מאחורי הניווט.
- פוקוס מקלדת, קריאות וניגודיות בשתי ערכות הנושא והפחתת תנועה.
- השוואה חזותית לפני ואחרי: האם התוספת משתמשת באותם צבעים, טיפוגרפיה, קווים וריווח ומשמרת את ההיררכיה?

## תחזוקת המסמך והקוד

לשפר רכיב קיים לפני המצאת וריאנט חדש. אם מתקבלת החלטה לשנות את השפה, לעדכן את המסמך במפורש יחד עם השינוי.

מקור הממשק הוא `kupa-sgura.html`. אין לערוך ידנית את `index.html` שנוצר באמצעות `build.py`, ואין לערוך את `poker-settle.html` הקפוא. תהליך שחרור מתועד ב־`HANDOFF.md`. שינוי תיעוד בלבד אינו דורש העלאת גרסה או יצירת build.


## תוספת גרסה 22 — פירוט כניסות

מתחת לשם השחקן בשולחן מוצג כפתור ״פירוט כניסות״: טקסט משני בגודל `12px` וחץ SVG קווי `12px` שמסתובב בפתיחה. האזור נפתח בתוך שורת השחקן, ללא modal, כרטיס או מסגרת נוספת, במעבר רשת של `.28s`.

כל רשומה מציגה שעה וסכום בלבד באותה שורה, כשהם קרובים זה לזה: `13px`, ספרות ברוחב קבוע, שעה בצד הימני בצבע `--dim` ומשקל `300`, קו קצר ועדין ביניהם, וסכום משמאלה בצבע `--text` ומשקל `500`. ריפוד אנכי `5px` ורווח `8px` סביב קו ההפרדה. שעה שלא תועדה בנתונים ישנים מוצגת כ־״—״; מצב ריק: ״אין כניסות עדיין״. יש לשמר פוקוס, `aria-expanded`, `aria-controls` והפחתת תנועה.

## סגירת שולחן מאוזנת

במסך החישוב ההפרש הוא מספר שלם של `סך כניסות − סך סגירות`. כאשר הוא `0`, כפתור הסגירה הרגיל פועל בזרימת האישור הקיימת. כאשר הוא חיובי, מוצג שהסכום חסר; כאשר הוא שלילי, מוצג שיש עודף. הכפתור הראשי ממורכז, בגודל קבוע ורחב מספיק לכל הניסוחים, ונראה חסום עם הנוסח ״לחיצה ארוכה כדי לסגור למרות הפער״. לחיצה רצופה של שנייה ממלאת אותו בצבע בהיר במצב כהה ובשחור במצב בהיר, מקצה ההתחלה הימני לקצה השמאלי, בגובה קבוע ועם קצוות מעוגלים; כשהמילוי מגיע לדפנות הוא מתחלף לטורקיז עם הנוסח ״סגור בכל זאת״. שחרור מוקדם מחזיר את המילוי במשך `.18s`; השלמה נשארת מלאה כ־`60ms` לפני החלפת המצב. לחיצה נוספת פותחת אישור inline עם ״חזור״ ו״סגור בכל זאת״. בעת הסגירה נשמרים `isBalanced` ו־`balanceDifference` ברשומת ההיסטוריה.

## חובות ותשלומים

בשורות ההעברה במסך החישוב מופיע כפתור קטן ״שולם״ עם toggle הפיך. סימון נשמר במשחק הנוכחי בלבד; בעת סגירה רק העברות שלא סומנו הופכות לרשומות חוב. בפרופיל מוצגות שתי רשימות שטוחות של חובות פתוחים: ״אני חייב״ ללא פעולת תשלום, ו״חייבים לי״ עם כפתור ״שולם״. תשלום מעדכן את רשומת החוב ל־`paid` ושומר `paidAt`, בלי לשנות את מאזן הפוקר או את היסטוריית המשחק. הסכום עצמו (`.debt-amount`) צבוע לפי כיוון החוב — טורקיז (`.pos`, `--accent`) ב״חייבים לי״, אדום (`.neg`, `--bad`) ב״אני חייב״ — כך שהשורה קריאה גם בלי להסתכל באיזו לשונית פנימית נמצאים; שתי הרשימות משתמשות באותו ניסוח מטא ״ממתין לתשלום״ (לא ״ממתין״ בקיצור באחת מהן).

בפרופיל, מתחת לשם המשתמש, יש בורר tabs עדין שבו ״מאזן״ נמצא בצד ימין ונבחר כברירת מחדל, ו״חובות״ בצד שמאל. כפתור החלפת המשתמש נשאר בהגדרות ואינו מופיע בגוף הפרופיל. בתוך לשונית ״חובות״ יש בורר פנימי רחב יותר של ״אני חייב״ ו״חייבים לי״, ורק הרשימה שנבחרה מוצגת. כשאין חובות פתוחות, מוצג הטקסט ״אין חובות פתוחות״ במרכז האזור. כאשר נכנס חוב פתוח חדש, מופיעה נקודה טורקיזית קטנה ליד לשונית החובות; היא נרשמת כנקראה ונעלמת אחרי הכניסה הראשונה לטאב.

## תוספת — קבוצות, חברים והזמנות

מתעד את מה שנבנה בסבב "קבוצות, חברים ומשחקי קבוצה" (`docs/superpowers/plans/2026-09-07-groups-foundation.md`,
משימות 1–18), ומעודכן לפי סבב העיצוב שבא אחריו (`.superpowers/sdd/2026-09-07-groups-foundation/design-round-brief.md`,
החלטות D1–D8 ותיקוני design-review P2/P3): שכבת הגדרות הקבוצה מרכזת כעת את ההזמנה והוספת החבר, שורת
חבר למנהל נפתחת בהקשה במקום פעולות גלויות תמיד, מסכי השולחן והסגירה מקבלים כותרת חזרה לקבוצה, וקבוצה
שעזבו אפשר להסיר מהמכשיר בשני שלבים. כל הרכיבים להלן משתמשים באותם משתני צבע, טיפוגרפיה ומרווחים
שכבר מוגדרים למעלה — אין צבע הדגשה שני, אין קופסה חדשה. שמות המחלקות והערכים נלקחו ישירות מה־CSS
הקיים.

### כרטיס קבוצה ומצבי הלוח

בלוח המשחקים, כרטיס קבוצה (`.games-group-card`) בנוי כמו כרטיס משחק פעיל: שורת ראש לחיצה
(`.games-group-head[role="button"]`) עם אווטאר עגול `34px` (תמונה או אות ראשונה על רקע `--line`),
שם וכפתור "הרחב" נפרד. כשיש לקבוצה משחק פתוח מופיעה נקודה טורקיזית ותווית "משחק פעיל" (אפורה
בשלב "בסגירה", מחלקה `.games-card-status.settlement`) לצד מונה החברים. ההרחבה (`.games-card-details`,
מעבר רשת `0fr → 1fr` ב־`.25s`) חושפת חברים, מספר משחקים, משחק אחרון ומוביל — טקסט בלבד, בלי סכומים.
קבוצות בארכיון מקבלות תווית "בארכיון" (`.games-member-tag`) ליד השם ומתקפלות תחת שורה נפרדת
"ארכיון (N)" (`.games-former-toggle`, אותו דפוס חץ מסתובב כמו "הרחב"), מתחת ל"הקבוצות שלי".

### כותרת עמוד קבוצה ופעולה ראשית

עמוד קבוצה (`.games-group-header`) ממורכז כמו שאר לוח המשחקים: אווטאר `64px`, שם `24px/800`,
ומטא `13px` דק ("N חברים"). הפעולה הראשית (`.games-primary-action`) היא אותו `.btn-primary` הקיים,
ברוחב מינימלי `200px`; כשההתחלה חסומה (`canStartGroupGame`) הכפתור מקבל `disabled` (אטימות `.45`,
מסגרת `--line`, טקסט `--faint`) ומתחתיו שורת סיבה שקטה (`.games-primary-reason`, `12px`, `--dim`,
דועכת פנימה). מצב "משחק פתוח" מציג שורת סטטוס (`.games-primary-status`, `13px/500`) לפני הכפתור.

כפתור ההגדרות הפינתי (`.games-group-settings-btn`) מציג כעת אייקון גלגל שיניים (אותו SVG כמו כפתור
ההגדרות בפרופיל) לצד המילה "הגדרות" — כך שהכניסה להגדרות נראית זהה בשני המקומות באפליקציה. קבוצה
שעזבתי (`!summary.isMember`) מציגה מתחת לכפתור הראשי החסום שורת סיבה כנה (`leftGroupReason`): "עזבת
את הקבוצה" כשיש לי שורת חברות במצב `left`, "הוסרת מהקבוצה" כשהיא `removed`, ומתחתיה כפתור
דו-שלבי אדום `.set-flat.danger` "מחק קבוצה" (`.games-hide-group-btn`), עם טקסט עזר "הקבוצה תוסר
מהמכשיר שלך. ההיסטוריה של שאר החברים נשמרת." אישור קורא ל־`hideGroupForMember`, שמסמן `hiddenAt` רק
על החברות שלי באותה קבוצה, ומחזיר ללוח המשחקים — הקבוצה נעלמת משם (`getGroupSummaries` מסנן קבוצות
מוסתרות) אך עדיין נפתחת כרגיל אם מגיעים אליה בקישור ישיר.

כלל: הכפתור "מחק קבוצה" בעמוד מוצג רק כשיש לי שורת חברות `left`/`removed` בקבוצה
(`findMyFormerMembership`), כלומר רק כשלחיצת האישור באמת יכולה להצליח. כשהשם שבמכשיר לא תואם לאף
שורת חברות (למשל שם תצוגה שהגיע מההתחברות ושונה מהשם ברשומות), העמוד לא טוען "עזבת" ולא מציע פעולה
הרסנית — הוא מציג רק "אתה לא חבר בקבוצה הזו — מחובר בשם X", כדי שאי־ההתאמה תהיה גלויה. לחיצת אישור
בכל דפוס דו-שלבי לעולם לא נגמרת ב־no-op שקט: אם הפעולה נדחית, המסך נדרך מחדש (disarm) ומתרנדר למצבו
הכנה במקום להשאיר את תווית "בטוח?" תלויה.

### בורר משתתפים (participant picker)

הפאנל נפתח עם כותרת מקטע "מי משחק?" (`.group-start-title`, אותה מחלקת כותרת מקטע כמו שאר האפליקציה)
ומונה חי "נבחרו N מתוך M" (`.group-start-counter`, `12px`, `--dim`) שמתעדכן במקום בכל סימון שורה,
בלי רינדור מחדש של כל הפאנל. כל עוד הפאנל פתוח, כפתור "התחל משחק" הראשי מוסתר — כדי שלא תהיה על
המסך יותר מקפסולה טורקיזית פעילה אחת בבת אחת — וחוזר להיראות כשהפאנל נסגר.

פאנל "התחל משחק" (`.group-start-panel`) הוא שורות `.group-start-member` בגובה מינימלי `44px`, כל
אחת עם תיבת סימון מרובעת `22px` (`.group-start-check`, פינות `6px`) שמתמלאת טורקיז ומציגה סימן ✓
לבן שדועך ומתרחב פנימה (`scale(.4)→1`) כשהשורה מסומנת. הוספת אורח (`.group-start-add`) פותחת שורת
שם + כפתור בתחתית הרשימה; אורחים מתווספים כ־`.group-start-guest-row` עם פעולת "הסר" בסוף השורה.
כפתור "פתח שולחן" (`.group-start-confirm`) חסום עד שנבחר משתתף אחד לפחות.

### שורות חברים ותוויות

חברים ומובילים חולקים שורה שטוחה אחת (`.games-leader-row`, `.games-member-row`) — שם `15px/600`,
קו הפרדה תחתון, בלי מסגרת. חבר מנהל מקבל תווית "מנהל" (`.games-member-tag`, `12px`, `--dim`, דועכת
פנימה); כנ"ל "לשעבר" בדירוג.

עבור מנהל, השורה עצמה היא הפקד (`role="button"`, `tabindex="0"`, `Enter`/`Space`, `aria-expanded`,
`aria-controls`) — אין עוד כפתורי פעולה גלויים תמיד בתוך השורה. הקשה עליה פותחת מתחתיה רצועת פעולות
אינליין באותו מעבר רשת `0fr → 1fr` כמו הרחבת היסטוריה (`.games-member-actions`, על גבי אותו
`.games-card-details`), עם חץ קטן (`.games-member-chevron`) שמסתובב `180deg` בפתיחה. רק שורה אחת
פתוחה בכל רגע — הקשה על שורה אחרת סוגרת את הקודמת. שורות חברים רגילים (לא מנהלים) אינן לחיצות ולא
נושאות פעולות.

הרצועה מציגה "הפוך למנהל" (`.games-member-promote`) לחבר רגיל, או "הסר ניהול" (אותה מחלקה, דרך
`renderRemoveAdminButton`) למנהל שאינו המנהל הפעיל האחרון בקבוצה, ולצדה "הסר" (`.games-member-remove`)
— פעולה הרסנית דו-שלבית, אותו דפוס "בטוח?" עם `.armed` שהופך אדום (`--bad`) כמו שאר האזהרות
באפליקציה. המנהל הפעיל האחרון לא מקבל אף אחת מהפעולות האלה — רק שורת הסבר שקטה "העבר ניהול לחבר אחר
קודם" (`.games-member-note`). איזו שורה פתוחה (`memberActionsOpenId`) הוא state שיוצא לגמרי מהתצוגה
(UI-only) ומתאפס בכל מעבר מסך.

חברים לשעבר מתקפלים תחת "חברים לשעבר (N)" (אותו `.games-former-toggle`).

### הוספת חבר

"+ הוסף חבר" (`.games-member-add-toggle`) עבר מעמוד הקבוצה לשכבת הגדרות הקבוצה, ומוצג רק למנהלים.
לחיצה פותחת פאנל אינליין באותו דפוס כמו יצירת קבוצה — כותרת מקטע "חבר חדש", שדה שם ברוחב מלא, כפתור
אישור "הוסף" וכפתור טקסט "ביטול" (`.games-create-cancel`) לצדו. מתחת לפאנל יושבת ההערה השקטה
המשותפת (`.games-member-add-note`, `12px`, `--dim`): "יעבוד כשהאפליקציה תתחבר לשרת" — אותה מחרוזת
בדיוק שמופיעה בבלוק ההזמנה, בלשונית חברים ובהודעת ההצטרפות (קבוע `SERVER_NOTE` אחד), במקום ארבע
ניסוחים שונים. היא מבהירה שהיום אפשר להוסיף רק אורח בשם, לא חשבון רשום. הוספת חבר מרעננת גם את עמוד
הקבוצה שמאחורי השכבה.

### בלוק הזמנה

בלוק ההזמנה עבר גם הוא מעמוד הקבוצה לשכבת הגדרות הקבוצה — הוא הסעיף הראשון בשכבה, ונשאר גלוי לכל
חבר פעיל (לא רק למנהלים), בדיוק כמו קודם. "הזמנה לקבוצה" (`.games-invite-card`) ממורכז: קוד גדול
(`.games-invite-code`, `28px/800`, ריווח אותיות `.12em`, ספרות ברוחב קבוע), שורת פעולות
(`.games-invite-action` — קפסולות גבול דק, גובה מינימלי `40px`: "העתק קישור", "שתף" כש־`navigator.share`
קיים). תא ה־QR הוסר לגמרי (כולל ה־CSS שלו) — במקומו נשארת רק ההערה השקטה המשותפת `SERVER_NOTE`; QR
אמיתי ייווצר מקישור ההזמנה (`inviteLink(...)`) כשיהיה שרת שיודע לממש אותו, וזה מתועד כהערת קוד במקום
placeholder חזותי. "הועתק ✓" (`.games-invite-copied`) דועך פנימה ליד כפתור ההעתקה. מנהל רואה גם "בטל
הזמנה" (`.games-invite-revoke`, מקבל צבע `--bad` ב־hover). כפתור יצירת ההזמנה הראשון
(`.games-invite-create`) הוא קפסולת גבול טורקיז כמו כפתור ראשי.

### היסטוריית קבוצה ודירוג מתרחב

שורת משחק בהיסטוריה (`.games-history-item`, ראש `.games-history-head` לחיץ) מציגה תאריך, מספר
שחקנים, מנצח/ים וקופה; משחק לא מאוזן מקבל תווית אדומה "לא מאוזן" (`.games-history-tag`, דועכת
פנימה). הקשה על השורה פותחת בתוך אותה שורה — לא ב־modal — דירוג לפי מקום (`.games-history-rank`,
`13px/500`, מוזח `padding-inline-start: 14px` ושמות המשתתפים בצבע `--dim` כדי שיובחנו מהמספר המודגש
בתחילת השורה) בסדר כניסה מדורג (`.anim`, `40–55ms` בין שורה לשורה), שמות בלבד, בלי כסף. שורות
"מובילים" (`renderGroupLeaders`) חולקות את אותה `.games-leader-row` וגם הן נכנסות במדרג.

### שכבת הגדרות קבוצה

"הגדרות קבוצה" (כפתור פינתי `.games-group-settings-btn`, פינה נגדית ל־`.back-arrow`, ואייקון גלגל
שיניים לצד המילה — ראו למעלה) פותחת שכבה מלאה שמשתמשת באותו `.login`/`.login-in.set-in` כמו הגדרות
המשתמש. השכבה גדלה — היא מכילה כעת גם את בלוק ההזמנה ואת פאנל הוספת החבר, שהוזזו לכאן מעמוד הקבוצה —
ולכן היא גוללת (`overflow-y: auto`, `-webkit-overflow-scrolling: touch`) עם ריפוד safe-area למעלה
ולמטה, במקום להיות ממורכזת בעמודה קבועה. סדר הסעיפים קבוע ולא תלוי תפקיד: (1) "הזמנה לקבוצה" — גלוי
לכל חבר פעיל; (2) "הוספת חבר" — למנהלים בלבד; (3) אווטאר עגול לעריכה (`.games-create-avatar` בהקשר
הזה, חסום כשאין הרשאת מנהל) ושדה שם — למנהלים בלבד; (4) שלוש פעולות ניהול שטוחות (`.set-flat`) —
"העבר לארכיון"/"שחזר מהארכיון", "מחק קבוצה" (`.danger`, דו-שלבי, "בטוח? ההיסטוריה הקבוצתית תוסתר",
ננעל אחרי `4s` כמו איפוס הרקורד), ו"עזוב קבוצה". שורת סיבה שקטה (`.games-primary-reason`) מתחת לכל
פעולה חסומה ("סגור קודם את המשחק הפעיל" / "מנה מנהל אחר קודם"). פעולות `.set-flat` נייטרליות עברו
לצבע `--text` (לא `--accent`) עם `hover` טורקיזי, כדי שלא יתחרו חזותית עם הפעולה ההרסנית האדומה; מצב
`disabled` עבר לצבע `--faint` בלבד, בלי `opacity`, לעקביות עם שאר המצבים החסומים באפליקציה.

### כותרת חזרה בשולחן ובסגירה

מסכי השולחן (`game`) והסגירה (`settle`) מקבלים שורת כותרת קומפקטית (`.table-header`) בתוך `<header>`,
מעל שורת ההוספה ושורות השחקנים, ומתחת לפקדי הפינה (איפוס/ערכת נושא) — היא יורשת מהם את הריפוד `96px`
שכבר שומר עליהם פנויים. חץ חזרה (`.back-arrow`) בפתיחתה, שם הקבוצה (`.table-header-name`, `17px/700`)
במרכז, ותווית שקטה מימין (`.table-header-label`, `12px`, `--dim`): "שולחן" ב־`game`, "סגירה" ב־`settle`.
משחק שנפתח מתוך קבוצה חוזר אליה (`openGroup(state.groupId)`); משחק ללא קבוצה מציג את אותה שורה עם
התווית "משחק ללא קבוצה" וחוזר ללוח המשחקים. שם הקבוצה, כנתון משתמש, נבנה תמיד כ־DOM node (`el(...)`)
ולא מוזרק ל־`innerHTML`. השורה "קבוצה: X" שהייתה בתחתית מסך השולחן (`renderDerived`) הוסרה — המידע
עבר לכותרת הזאת בראש המסך. השורה נכנסת באנימציית `rise` כמו כותרת עמוד הקבוצה.

### תג ופאנל יציאת שחקן

בשורת שחקן פעיל, "יציאה" (`.exit-toggle`, `12px`, `--bad`, מטרת מגע `44px`) יושבת ליד "פירוט כניסות".
לחיצה פותחת אותו מעבר רשת `.pmenu`/`.pmenu-in` עם שדה סכום שעטוף כעת בקונטיינר `.exit-field` — קו
תחתון אחד לכל הקונטיינר (לא לקלט עצמו) שנעשה טורקיז ב־`:focus-within`, עם תווית "₪" קבועה
(`.exit-currency`, `--dim`) לצד הקלט (`input.exit-cashout-input`; ה־placeholder ותווית ה־aria הם
"כמה יצא איתו", לא "צ'יפים ביציאה"). לצד השדה כפתור "אשר יציאה" (`.exit-confirm`, קפסולת גבול אדומה)
ופעולת טקסט "ביטול" (`.exit-cancel`, `--dim`) שסוגרת את הפאנל בלי לשמור. שחקן שיצא מקבל שם דהוי
(`.prow.exited .pname`, אטימות `.55`), תג שקט "יצא · ₪X" (`.exit-tag`, דועך פנימה) ופעולת "עריכה"
(`.exit-edit`) שפותחת מחדש את אותו פאנל עם הסכום הקיים — מופרדת מהתג בקו הפרדה אנכי דק
(`border-inline-start`, `1px`, `--line`) ורווח `12px`, במקום נקודה כמו שאר צירופי המטא באפליקציה.

בהוספת שחקן, הכניסה הראשונה (`MIN_BUYIN`) נרשמת מיד והשורה נכנסת באנימציה הרגילה; תפריט ה־Rebuy נשאר
סגור. פתיחתו היא פעולה מפורשת של כפתור הפלוס (`.btn-plus`) בלבד, גם בהוספה דרך צ׳יפ של חבר קבוצה.

### מסך חברים ראשי

היעד הראשי "חברים" (`appView === "friends"`) נמצא בקצה הימני של הניווט התחתון. הפרופיל מכיל רק
"מאזן" ו"חובות". המצב האמיתי היום, כי אין נתיב ב־UI שיוצר בקשת
חברות, הוא ריק: מוצגת הודעה שקטה אחת "עוד אין חברים" (`.empty-note`) — לא שלוש רשימות ריקות. כשיהיו
נתונים היא תציג שלוש קבוצות `.debt-row` שטוחות דרך `renderFriendGroup` ("חברים", "בקשות שהתקבלו",
"בקשות שנשלחו"), עם אותה כניסה מדורגת `45ms` לשורה כמו רשימת חובות. מתחת לרשימה (או להודעה הריקה)
יושב כפתור "הוסף חבר" חסום (`disabled`, `aria-disabled`) ומתחתיו הערה שקטה משותפת (`.friend-helper`,
`SERVER_NOTE`) — "יעבוד כשהאפליקציה תתחבר לשרת", אותה מחרוזת בדיוק שמופיעה בבלוק ההזמנה, בפאנל
הוספת חבר ובהודעת ההצטרפות.

### תנועה שנוספה (Task 18)

לא נוספה משפחת אנימציה חדשה — רק שימוש עקבי במה שכבר קיים, ותיקון מקומות שהיו קופצים בלי מעבר:
כניסת שורה (`rise`, `.3–.4s`), מעבר רשת לפתיחת פאנל (`0fr → 1fr`, `.25–.28s`), הדגשת טקסט/תג שמופיע
(`fadeIn`, `.25s` — קלטת חדשה, משמשת "הועתק ✓", "יצא", תוויות מנהל/לשעבר/בארכיון, שורות סיבה),
כיווץ לחיצה (`scale(.92)` לכפתורים, `scale(.97)` לשורות `role="button"` כמו ראש כרטיס קבוצה ושורת
היסטוריה), וחץ מסתובב `180deg` בפתיחת "הרחב"/"חברים לשעבר"/"ארכיון". מדרג שורות (40–55ms בין שורה
לשורה) חל על שורות מובילים, דירוג היסטוריה מורחב, ובורר המשתתפים. מעבר בין מסכים (`games ↔ group ↔
game ↔ settle ↔ profile`) מקבל הבזק כניסה קצר על הקונטיינר הנראה (שימוש חוזר ב־`.load-in`, מוסר
ב־`animationend`) — לא רק במסך הראשון. כל התנועה כפופה לכלל הגלובלי הקיים: תחת
`prefers-reduced-motion: reduce` כל `animation`/`transition` מבוטלים; זה נשאר הכלל האחרון בגיליון
הסגנונות ואסור לעקוף אותו ברכיב בודד.
